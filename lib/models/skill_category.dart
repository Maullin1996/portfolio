/// A labeled group of related skills (e.g. "Mobile", "Testing") shown as a
/// block of chips in the "Sobre mí" section.
class SkillCategory {
  final String label;
  final List<String> skills;

  const SkillCategory({required this.label, required this.skills});
}
